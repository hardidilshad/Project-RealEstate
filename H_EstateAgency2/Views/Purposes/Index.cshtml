@model IEnumerable<H_EstateAgency2.Models.Purpose>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.purposefor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.purposefor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.purposeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.purposeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.purposeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
